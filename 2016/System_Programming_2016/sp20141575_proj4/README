1.python library인 requests 와 BeautifulSoup4가 없다면  requests를 설치하기 위해 $pip install --user requests 를 입력해주고 BeautifulSoup4를 설치하기 위해 $pip install --user BeautifulSoup4 를 입력해준다.  library가 설치 되어 있다면 2번 과정으로 넘어간다.
2.python 20141575.py를 입력하여 실행한다.
3.2번 과정으로 생긴 URL.txt에는 cspro.sogang.ac.kr/~gr120160213의 하이퍼 링크로 연결된 URL들을 방문한 page의 URL을 줄단위로 출력되어 있고, Output_0001.txt ~ Output_0023.txt에는 방문한 순서대로 page들의 text들이 저장되어 있다.
